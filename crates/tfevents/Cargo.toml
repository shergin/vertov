[package]
name = "tfevents"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Read TensorBoard tfevents files: TFRecord framing and minimal Event decoding, zero dependencies"
keywords = ["tensorboard", "tfevents", "tfrecord", "parser"]
categories = ["parser-implementations", "encoding"]
readme = "README.md"

[dev-dependencies]
criterion = "0.5"

[[bench]]
name = "cold_load"
harness = false
